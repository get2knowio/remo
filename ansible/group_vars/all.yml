---
# Hetzner Cloud Configuration
hetzner_api_token: "{{ lookup('env', 'HETZNER_API_TOKEN') }}"
hetzner_server_name: "remo"
hetzner_server_type: "cx23"  # Smallest shared vCPU (2 vCPU, 4GB RAM) - cheapest option
hetzner_image: "ubuntu-24.04"
hetzner_location: "hel1"  # Falkenstein datacenter
hetzner_ssh_key_name: "remote-coding-key"

# Volume settings for persistent home directory
hetzner_volume_name: "{{ hetzner_server_name }}-home"
hetzner_volume_size: 20  # GB (minimum 10)

# AWS Configuration
aws_access_key_id: "{{ lookup('env', 'AWS_ACCESS_KEY_ID') }}"
aws_secret_access_key: "{{ lookup('env', 'AWS_SECRET_ACCESS_KEY') }}"
aws_region: "{{ lookup('env', 'AWS_REGION') | default('us-west-2', true) }}"

# User Configuration
remo_user: "remo"

# Development workspace directory (used by devshell script)
dev_workspace_dir: "/home/{{ remo_user }}/projects"

# Zellij Configuration
zellij_session_name: "main"  # Session name for SSH auto-attach

# Node.js Configuration
nodejs_version: "24"  # LTS version

# Credential broker configuration
remo_broker_user: "remo-broker"
remo_broker_group: "remo-broker"
remo_broker_protocol_version: 2
remo_broker_binary_path: "/usr/local/bin/remo-broker"
remo_broker_config_dir: "/etc/remo-broker"
remo_broker_config_path: "/etc/remo-broker/config.toml"
remo_broker_projects_dir: "/etc/remo-broker/projects"
remo_broker_socket_dir: "/run/remo-broker"
remo_broker_admin_socket: "/run/remo-broker/admin.sock"
remo_broker_log_dir: "/var/log/remo-broker"
remo_broker_audit_log: "/var/log/remo-broker/audit.log"
remo_broker_bootstrap_token_path: "/etc/remo-broker/bootstrap-token"
