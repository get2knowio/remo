"""Vendored JSON Schemas (baseline) for cross-repo contracts.

The authoritative schemas are published per release by `get2knowio/remo-broker`.
Files here are the fallback baselines bundled into the Remo wheel.
"""
